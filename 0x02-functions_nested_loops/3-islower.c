#include "main.h"

/**
 * _islower -> function that checks if characters are in lower case
 * @c: a character argument
 * return: returns 1 and 0, which is dependent on comdition given
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z'); 
}
